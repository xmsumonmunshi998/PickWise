package com.example.ui.theme

import androidx.compose.ui.graphics.Color

// PickWise Design System Colors (matching reference UI)
val PickWiseNavy = Color(0xFF0F172A)         // Slate 900 - dark navy
val PickWiseNavyDark = Color(0xFF0A1128)     // Deep space navy for Splash
val PickWiseBlue = Color(0xFF2563EB)         // Royal Blue for buttons/actions
val PickWiseBlueDark = Color(0xFF1D4ED8)     // Darker blue
val PickWiseBlueLight = Color(0xFF3B82F6)    // Light blue
val PickWiseBlueContainer = Color(0xFFEFF6FF)// Soft ice-blue for cards & badges
val PickWiseTeal = Color(0xFF0D9488)         // AI match gauge / score
val PickWiseTealLight = Color(0xFFF0FDFA)    // Soft teal container
val PickWiseGreen = Color(0xFF10B981)        // Positive checks / Best Deal
val PickWiseGreenLight = Color(0xFFECFDF5)   // Light green badge background
val PickWiseOrange = Color(0xFFF59E0B)       // Ratings & badges
val PickWiseRed = Color(0xFFEF4444)          // Complaints
val PickWiseRedLight = Color(0xFFFEF2F2)     // Complaints background
val PickWiseSurface = Color(0xFFFFFFFF)      // Pure white surface
val PickWiseBackground = Color(0xFFF8FAFC)   // Slate 50 background
val PickWiseCardBorder = Color(0xFFE2E8F0)   // Slate 200 subtle divider
val PickWiseTextPrimary = Color(0xFF0F172A)  // Deepest slate text
val PickWiseTextSecondary = Color(0xFF64748B)// Secondary slate text
val PickWiseTextMuted = Color(0xFF94A3B8)    // Muted text
